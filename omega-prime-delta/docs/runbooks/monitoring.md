# Monitoring

- Track risk-engine leadership transitions from service logs (`Became risk engine leader`).
- Alert if `/health` fails for more than 30 seconds.
- Alert on rising order rejection rates (`/validate` 4xx responses).
