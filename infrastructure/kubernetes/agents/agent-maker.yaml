apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-maker
  namespace: omega-prime
  labels:
    app: agent-maker
    tier: strategy-agent
    version: v9.0
spec:
  replicas: 2
  selector:
    matchLabels:
      app: agent-maker
  template:
    metadata:
      labels:
        app: agent-maker
        tier: strategy-agent
    spec:
      containers:
        - name: agent-maker
          image: omega-prime/agent-maker:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: KAFKA_BOOTSTRAP
              value: "kafka:9092"
            - name: CONSUME_TOPIC
              value: "market.ticks"
            - name: PRODUCE_TOPIC
              value: "agents.maker.signals"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: agent-maker
  namespace: omega-prime
spec:
  selector:
    app: agent-maker
  ports:
    - name: http
      port: 80
      targetPort: 8000
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: agent-maker-hpa
  namespace: omega-prime
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: agent-maker
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
