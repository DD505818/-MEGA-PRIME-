apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-ceo
  namespace: omega-prime
  labels:
    app: agent-ceo
    tier: strategy-agent
    version: v9.0
spec:
  replicas: 2
  selector:
    matchLabels:
      app: agent-ceo
  template:
    metadata:
      labels:
        app: agent-ceo
        tier: strategy-agent
    spec:
      containers:
        - name: agent-ceo
          image: omega-prime/agent-ceo:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: KAFKA_BOOTSTRAP
              value: "kafka:9092"
            - name: CONSUME_TOPIC
              value: "all.agent.signals"
            - name: PRODUCE_TOPIC
              value: "agents.ceo.commands"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: agent-ceo
  namespace: omega-prime
spec:
  selector:
    app: agent-ceo
  ports:
    - name: http
      port: 80
      targetPort: 8000
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: agent-ceo-hpa
  namespace: omega-prime
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: agent-ceo
  minReplicas: 2
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
