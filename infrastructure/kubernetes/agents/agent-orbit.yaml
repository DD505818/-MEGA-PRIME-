apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-orbit
  namespace: omega-prime
  labels:
    app: agent-orbit
    tier: strategy-agent
    version: v9.0
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-orbit
  template:
    metadata:
      labels:
        app: agent-orbit
        tier: strategy-agent
    spec:
      containers:
        - name: agent-orbit
          image: omega-prime/agent-orbit:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: KAFKA_BOOTSTRAP
              value: "kafka:9092"
            - name: CONSUME_TOPIC
              value: "features.volatility"
            - name: PRODUCE_TOPIC
              value: "agents.orbit.signals"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: agent-orbit
  namespace: omega-prime
spec:
  selector:
    app: agent-orbit
  ports:
    - name: http
      port: 80
      targetPort: 8000
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: agent-orbit-hpa
  namespace: omega-prime
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: agent-orbit
  minReplicas: 1
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
