apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-mev-hunter
  namespace: omega-prime
  labels:
    app: agent-mev-hunter
    tier: strategy-agent
    version: v9.0
spec:
  replicas: 2
  selector:
    matchLabels:
      app: agent-mev-hunter
  template:
    metadata:
      labels:
        app: agent-mev-hunter
        tier: strategy-agent
    spec:
      containers:
        - name: agent-mev-hunter
          image: omega-prime/agent-mev-hunter:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: KAFKA_BOOTSTRAP
              value: "kafka:9092"
            - name: CONSUME_TOPIC
              value: "market.ticks"
            - name: PRODUCE_TOPIC
              value: "agents.mev.signals"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: agent-mev-hunter
  namespace: omega-prime
spec:
  selector:
    app: agent-mev-hunter
  ports:
    - name: http
      port: 80
      targetPort: 8000
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: agent-mev-hunter-hpa
  namespace: omega-prime
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: agent-mev-hunter
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
