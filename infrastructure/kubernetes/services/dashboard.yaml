apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard
  namespace: omega-prime
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard
  template:
    metadata:
      labels:
        app: dashboard
    spec:
      containers:
        - name: dashboard
          image: omega-prime/dashboard:latest
          ports:
            - containerPort: 3000
