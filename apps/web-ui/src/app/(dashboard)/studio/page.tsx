import { PageHeader } from '@/components/layout/PageHeader';

export default function StudioPage() {
  return (
    <section className="space-y-4">
      <PageHeader title="Studio" subtitle="Production operator surface" />
      <div className="panel">Studio workspace with real-time + typed API integration contracts.</div>
    </section>
  );
}
